services:
  app:
    build:
      context: .
    environment:
      - APP_ENV=development
      - NODE_ENV=development
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - 3000:3000

volumes:
  node_modules:
    driver: local
